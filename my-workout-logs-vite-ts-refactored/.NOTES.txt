. Avoid using nanoid() in keys like in <CalendarDay> at Calendar.tsx
. Improve this app with dashboard
. Sign in with google account

Note: Unlike Create React App, Vite does not automatically inject environment variables into your application's build process. You need to manually access them using import.meta.env. Additionally, only variables prefixed with VITE_ are exposed to your Vite project by default.

=> Google Cloud specific localhost port only required to run dev.



Refactoring:

 . useState() updater function (setSomething()) is stable, so React does not require it in dependency arrays, remove it where necessary.


Issues: 

. When on subsequent months, <ActualDate> failed to bring me back to current date (2026-03-31) an actually going to (2026-04-01) one day after.

. Password need to be cleared when known user with saved password clicks create new user in login.